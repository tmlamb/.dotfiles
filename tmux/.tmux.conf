set -ga terminal-overrides ",screen-256color*:Tc"
set-option -g default-terminal "screen-256color"
set -s escape-time 0

set -g status-style 'bg=#333333 fg=#bba2fd'
set -g status-right '#H - #S'

bind r source-file ~/.tmux.conf
set -g base-index 1

set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

bind -r D neww -c "#{pane_current_path}" "[[ -e TODO.md ]] && nvim TODO.md || nvim ~/personal/notes/todo.md"

bind-key -n C-f run-shell "tmux neww ~/.local/scripts/tmux-sessionizer"

bind-key -r P run-shell "~/.local/scripts/tmux-sessionizer ~/workspaces/probable-pitchers"
bind-key -r A run-shell "~/.local/scripts/tmux-sessionizer ~/workspaces/activity-log-tracker"

# As per neovim :checkhealth warnings to fix tmux
set-option -sa terminal-features ',xterm-256color:RGB'
set-option -g focus-events on

bind -n S-Enter send-keys Escape "[13;2u"
